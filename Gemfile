# this file, always named "Gemfile" (no suffix), informs other users of dependencies for running these files
# to install these gem dependencies in your computer, run the "bundle install" command in the terminal

source "https://rubygems.org"

gem "activesupport"
gem "pry-byebug"
